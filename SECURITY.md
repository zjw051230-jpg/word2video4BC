# Security

不要提交 API Key、Cookie、访问令牌、真实项目素材、客户剧本、生成成片、数据库、任务日志或生产快照。

API Key 应只通过安装后的 `Configure-SeedanceApi.ps1` 写入本机工作区 `4.apis`。发现密钥误提交时，应立即撤销该密钥，再清理 Git 历史；仅删除最新文件不足以消除泄露。

公开问题中不要粘贴密钥或私有素材。安全问题请通过 GitHub 仓库的私密安全报告功能联系维护者。

